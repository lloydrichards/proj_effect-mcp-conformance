{
  "name": "@repo/scenario-ping",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/mcp-fixture": "workspace:*"
  }
}
